@extends('admin.layouts.content')@section('content')    <div class="col-md-12 col-sm-12 col-xs-12">        <div class="x_panel">            <div class="x_content">                <div class="table-responsive">                    <table class="table table-striped jambo_table bulk_action ">                        <thead>                        <tr class="headings">                            <th class="column-title">Id </th>                            <th class="column-title">Name </th>                            <th class="column-title">Delete </th>                        </tr>                        </thead>                        <tbody>                        @if(!empty($flowers))                            <a href="{{ route('admin.flowers.create') }}"><button type="button" class="btn btn-primary">Create Item</button></a>                        @foreach($flowers as $flower)                            <tr class="even pointer">                                <td class=" ">{{ $flower->id }}</td>                                <td class=" ">{{ $flower->name }}</td>                                <td><a href="{{  route('admin.flowers.destroy', $flower->id )}}"><button class="btn btn-danger"><i class="fa fa-times"></i></button></a></td>                            </tr>                        @endforeach                        @endif                        </tbody>                    </table>                    @if(!empty($flowers))                    {{ $flowers->appends(['sort' => 'votes'])->links() }}                        @endif                </div>            </div>        </div>        <script>            $("#check-all").click(function(){                console.log('zz');                $('input:checkbox').not(this).prop('checked', this.checked);            });        </script>  </div>@endsection